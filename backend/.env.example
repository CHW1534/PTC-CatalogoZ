# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_password
DB_DATABASE=ptcretail

# Application
PORT=3000
NODE_ENV=development

# Seed (set to true to populate initial data on startup)
RUN_SEED=true
